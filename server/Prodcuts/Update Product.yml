info:
  name: Update Product
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:3000/api/products/1
  body:
    type: json
    data: |-
      {
        "productName": "Leaf Rake Pro",
        "price": 29.95,
        "starRating": 4.8
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
